{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Both app windows may use core APIs (events, window info).",
  "windows": ["strip", "flyout"],
  "permissions": ["core:default"]
}
